# User policy defaults

New users should be non-admin by default.

Recommended defaults:

- admin disabled
- account enabled
- content deletion disabled
- remote control of other users disabled
- shared device control disabled unless needed
- library/channel access set intentionally for your server

Review Jellyfin policy fields after upgrades because upstream behavior can change.
